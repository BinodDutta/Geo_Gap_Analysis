# Auto detect text files and perform LF normalization
* text=auto
# Auto-detect text files and perform LF normalization
* text=auto

# Ensure python scripts always use LF (Linux style) - safer for everyone
*.py text eol=lf

# Treat your massive data files as binary so Git doesn't crash trying to read them
*.csv binary
*.json binary
*.geojson binary

# Treat the HTML map as binary (so Git doesn't try to show diffs for 10,000 lines of map code)
*.html binary

# Ignore generated HTML maps from language stats
*.html linguist-generated=true

# Ignore JSON data files
*.json linguist-generated=true

# Ignore CSV data files
*.csv linguist-generated=true

# Force Python to be detected (optional but good)
*.py linguist-detectable=true